{
  "name": "my-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A general-purpose AI agent: Node/TypeScript backend + React web console",
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"pnpm --filter server dev\" \"pnpm --filter web dev\"",
    "dev:server": "pnpm --filter server dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm --filter server build && pnpm --filter web build",
    "db:migrate": "pnpm --filter server db:migrate",
    "start": "bash scripts/start.sh",
    "stop": "bash scripts/stop.sh",
    "restart": "bash scripts/restart.sh",
    "start:server": "pnpm --filter server start",
    "test": "pnpm --filter server test",
    "typecheck": "pnpm --filter server typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
